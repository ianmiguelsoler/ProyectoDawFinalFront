services:
  frontend:
    build:
      context: .
      dockerfile: docker/production/Dockerfile
    container_name: frontend_prod
    restart: always
    volumes:
      - ./public_html:/var/www/html  # ✅ Se monta correctamente el frontend
    ports:
      - "8081:80"
