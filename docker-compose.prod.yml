version: '3.8'
services:
  frontend:
    build: ./docker/production
    container_name: frontend_prod
    restart: always
    volumes:
      - ./public_html:/var/www/html 
    ports:
      - "3000:80"
